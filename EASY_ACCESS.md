# Easy Access to AKIR Restaurant Website

## Quick start
```bash
npm install
npm run dev
```

Open `http://localhost:5175`.

## With local backend
Add this to `.env.local` before starting the frontend:

```env
VITE_API_URL=http://localhost:4000
```

Keep the backend running separately from the backend project.
